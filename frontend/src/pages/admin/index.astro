---
import BaseLayout from "../../layouts/BaseLayout.astro";
import AdminDashboard from "../../components/admin/AdminDashboard.tsx";
---

<BaseLayout
  title="Dashboard"
  description="Post and manage class announcements."
  canonicalPath="/admin"
  noindex={true}
>
  <div class="page-heading">
    <h1>Dashboard</h1>
  </div>

  <!--
    client:only - the dashboard is entirely client-driven (JWT in memory), so
    there is nothing useful to server-render and no session on the server.
  -->
  <AdminDashboard client:only="react" />

  <noscript>
    <p class="alert alert--error">
      The dashboard needs JavaScript. Classmates can still read every
      announcement without it.
    </p>
  </noscript>
</BaseLayout>

<style is:global>
  /* ------------------------------------------------------------- modals */
  .modal {
    position: fixed;
    inset: 0;
    z-index: 80;
    background: rgba(9, 12, 32, 0.55);
    backdrop-filter: blur(2px);
    display: grid;
    place-items: center;
    padding: 20px;
    overflow-y: auto;
  }

  .modal__panel {
    background: var(--paper);
    border-radius: var(--radius);
    box-shadow: var(--shadow-lg);
    width: 100%;
    max-height: calc(100vh - 40px);
    max-height: calc(100dvh - 40px);
    display: flex;
    flex-direction: column;
    animation: modal-in 0.16s ease-out;
  }

  @keyframes modal-in {
    from {
      opacity: 0;
      transform: translateY(8px) scale(0.99);
    }
  }

  .modal__panel--sm {
    max-width: 440px;
  }

  .modal__panel--md {
    max-width: 620px;
  }

  .modal__panel--lg {
    max-width: 820px;
  }

  /* The poster maker needs room for the form and the preview side by side. */
  .modal__panel--xl {
    max-width: 1120px;
  }

  .modal__header {
    display: flex;
    align-items: center;
    gap: 12px;
    padding: 16px 20px;
    border-bottom: 1px solid var(--line);
    background: var(--brand-50);
    border-radius: var(--radius) var(--radius) 0 0;
  }

  .modal__title {
    font-size: 1.08rem;
    margin: 0;
    flex: 1;
    min-width: 0;
    overflow: hidden;
    text-overflow: ellipsis;
    white-space: nowrap;
  }

  .modal__close {
    border: none;
    background: transparent;
    font-size: 1.7rem;
    line-height: 1;
    color: var(--muted);
    cursor: pointer;
    padding: 0 4px;
  }

  .modal__close:hover:not(:disabled) {
    color: var(--brand-800);
  }

  .modal__body {
    padding: 20px;
    overflow-y: auto;
  }

  .modal__footer {
    display: flex;
    justify-content: flex-end;
    gap: 10px;
    padding: 14px 20px;
    border-top: 1px solid var(--line);
    background: var(--brand-50);
    border-radius: 0 0 var(--radius) var(--radius);
  }

  /* ---------------------------------------------------------- admin bar */
  .admin-bar {
    display: flex;
    flex-wrap: wrap;
    gap: 14px;
    align-items: center;
    justify-content: space-between;
    background: var(--paper);
    border: 1px solid var(--line);
    border-radius: var(--radius);
    padding: 16px 18px;
    margin-bottom: 18px;
    box-shadow: var(--shadow-sm);
  }

  .admin-bar__who {
    margin: 0;
    font-size: 0.92rem;
  }

  .admin-bar__actions {
    display: flex;
    gap: 8px;
    flex-wrap: wrap;
    align-items: center;
  }

  /* A hairline before the housekeeping buttons, so "New announcement" reads as
     the one thing this page is for. */
  .admin-bar__divider {
    width: 1px;
    align-self: stretch;
    min-height: 26px;
    background: var(--line);
    margin: 0 2px;
  }

  @media (max-width: 700px) {
    .admin-bar__divider {
      display: none;
    }
  }

  .searchbar {
    display: flex;
    gap: 8px;
    margin-bottom: 18px;
    flex-wrap: wrap;
  }

  .searchbar .input {
    max-width: 340px;
  }

  /* .select inherits width:100% from the shared field styles, which would put
     each dropdown on a row of its own inside this flex bar. */
  .searchbar .select {
    width: auto;
    min-width: 170px;
    flex: 0 1 auto;
  }

  /* -------------------------------------------------------- admin table */
  .table-scroll {
    overflow-x: auto;
    background: var(--paper);
    border: 1px solid var(--line);
    border-radius: var(--radius);
    box-shadow: var(--shadow-sm);
  }

  .admin-table {
    width: 100%;
    border-collapse: collapse;
    font-size: 0.92rem;
    /* Fixed layout, so the columns below get the widths they are given.
       Under auto layout every nowrap column took what it wanted and the
       title - the only wrappable one - was squeezed to a word per line. */
    table-layout: fixed;
    /* With the row actions behind one button the table fits the page, so
       there is no sideways scrolling left to do at full width. */
    min-width: 680px;
  }

  /* Pixel widths for the columns whose content is a known size, and `auto`
     for the title so it absorbs whatever is left - and grows on a wide
     screen instead of everything stretching. Percentages were guesswork:
     they left the four action buttons a few pixels short of fitting. */
  .admin-table th:nth-child(1) { width: auto; }   /* title    - the remainder */
  .admin-table th:nth-child(2) { width: 150px; }  /* filed under */
  .admin-table th:nth-child(3) { width: 112px; }  /* status */
  .admin-table th:nth-child(4) { width: 125px; }  /* updated */
  .admin-table th:nth-child(5) { width: 64px; }   /* the actions menu button */

  /* Publishers: three buttons, and "Resend invite" is the long one. */
  .admin-table--compact th:nth-child(1) { width: auto; }   /* account */
  .admin-table--compact th:nth-child(2) { width: 150px; }  /* status */
  .admin-table--compact th:nth-child(3) { width: 70px; }   /* posts */
  .admin-table--compact th:nth-child(4) { width: 285px; }

  .admin-table th,
  .admin-table td {
    text-align: left;
    padding: 14px;
    border-bottom: 1px solid var(--line);
    vertical-align: middle;
  }

  /* Scanning a row of six columns is easier with the row itself responding. */
  .admin-table tbody tr {
    transition: background 0.12s ease;
  }

  .admin-table tbody tr:hover {
    background: var(--brand-50);
  }

  .admin-table th {
    background: var(--brand-50);
    font-size: 0.76rem;
    letter-spacing: 0.07em;
    text-transform: uppercase;
    color: var(--muted);
    /* "Posted by" was folding onto two lines and deepening every header. */
    white-space: nowrap;
  }

  /* The Publishers table has four columns, not six, and lives inside an
     820px modal - the dashboard's min-width would make it scroll sideways for
     no reason. */
  .admin-table--compact {
    /* Fits inside the 820px modal without scrolling, while leaving room for
       "Resend invite", "Disable" and "Remove" on one line. */
    min-width: 700px;
  }

  /* The badges are nowrap already, so they need no minimum of their own -
     one was previously stealing width from the title. */
  .admin-table__filed .tag {
    margin: 2px 4px 2px 0;
  }

  .admin-table tr:last-child td {
    border-bottom: none;
  }

  .admin-table__title {
    display: block;
    font-weight: 600;
    color: var(--brand-800);
    text-decoration: none;
  }

  .admin-table__title:hover {
    text-decoration: underline;
  }

  .admin-table__nowrap {
    white-space: nowrap;
    color: var(--muted);
    font-size: 0.86rem;
  }

  .admin-table__actions {
    display: flex;
    gap: 5px;
    justify-content: flex-end;
    flex-wrap: nowrap;
  }

  /* Tighter than a standalone button: four of these share one column. */
  .admin-table__actions .btn {
    white-space: nowrap;
    padding: 6px 9px;
  }

  /* ------------------------------------------------- row actions menu */
  .row-menu {
    padding: 4px 10px;
    font-size: 1.05rem;
    line-height: 1.1;
    letter-spacing: 0.06em;
  }

  /*
    Fixed, not absolute: the table lives in a horizontal scroller, and a panel
    positioned inside it would be clipped at the edge. The component measures
    the button and places this against the viewport.
  */
  .row-menu__panel {
    position: fixed;
    z-index: 90;
    min-width: 190px;
    padding: 6px;
    background: var(--paper);
    border: 1px solid var(--line);
    border-radius: var(--radius-sm);
    box-shadow: var(--shadow-md);
    display: flex;
    flex-direction: column;
    animation: modal-in 0.12s ease-out;
  }

  .row-menu__item {
    appearance: none;
    border: none;
    background: transparent;
    font: inherit;
    font-size: 0.9rem;
    text-align: left;
    padding: 9px 12px;
    border-radius: 7px;
    color: var(--ink);
    cursor: pointer;
    white-space: nowrap;
  }

  .row-menu__item:hover,
  .row-menu__item:focus-visible {
    background: var(--brand-50);
    outline: none;
  }

  .row-menu__item--danger {
    color: var(--danger);
  }

  .row-menu__item--danger:hover,
  .row-menu__item--danger:focus-visible {
    background: var(--danger-soft);
  }

  .row-menu__rule {
    height: 1px;
    background: var(--line);
    margin: 5px 8px;
  }

  /* The "preview is missing" explainer in the Share dialog. */
  .share-help {
    margin-top: 16px;
    border-top: 1px solid var(--line);
    padding-top: 12px;
  }

  .share-help > summary {
    cursor: pointer;
    font-size: 0.85rem;
    font-weight: 600;
    color: var(--brand-700);
  }

  .share-help p {
    font-size: 0.85rem;
    color: var(--muted);
    margin: 10px 0 0;
  }

  /* A long unbroken title must wrap rather than widen the fixed column. */
  .admin-table__title {
    overflow-wrap: anywhere;
  }

  button.tag {
    cursor: pointer;
    font-family: inherit;
  }

  /* -------------------------------------------------------- attachments */
  .upload-panel {
    background: var(--brand-50);
    border: 1px solid var(--line);
    border-radius: var(--radius-sm);
    padding: 16px;
  }

  .upload-panel__row {
    display: flex;
    gap: 12px;
    flex-wrap: wrap;
    margin-bottom: 14px;
  }

  /* ---------------------------------------------------------- poster maker */
  .poster {
    display: grid;
    /* The page is landscape now, so the preview earns a wider column. */
    grid-template-columns: minmax(0, 1fr) minmax(0, 500px);
    gap: 24px;
    align-items: start;
  }

  .poster__form .field:last-child {
    margin-bottom: 0;
  }

  .poster__textarea {
    min-height: 0;
  }

  /*
    Lists and schedules stay monospaced - the columns and the indents that
    drive the layout are only visible when the characters line up. A plain
    paragraph is just a paragraph.
  */
  .poster__textarea--prose {
    font-family: var(--font);
    font-size: 0.95rem;
    line-height: 1.5;
  }

  /* The preview follows the form down a long list of fields. */
  .poster__preview {
    position: sticky;
    top: 0;
  }

  .poster__stage {
    background: var(--brand-50);
    border: 1px solid var(--line);
    border-radius: var(--radius-sm);
    padding: 12px;
    display: grid;
    place-items: center;
  }

  /*
    The canvas is drawn at 1200px wide whatever the preview is; letting CSS
    scale it keeps the poster crisp on the screen and full size in the file.
  */
  .poster__canvas {
    width: 100%;
    height: auto;
    box-shadow: var(--shadow-sm);
    background: var(--paper);
  }

  /* ------------------------------------------- poster row editors */
  /*
    A schedule is entered as a row of boxes per subject. The columns are named
    once in a header strip above them, so each row is just the values.
  */
  .poster-rows {
    display: grid;
    gap: 8px;
    margin-bottom: 10px;
  }

  /* The column widths come from the field itself - a schedule may have three
     columns or five - and are set inline. */
  .poster-rows__head,
  .poster-rows__row {
    display: grid;
    gap: 8px;
    align-items: center;
  }

  .poster-rows__head {
    font-size: 0.76rem;
    font-weight: 600;
    letter-spacing: 0.04em;
    text-transform: uppercase;
    color: var(--muted);
  }

  .poster-rows__cell {
    display: block;
    min-width: 0;
  }

  /* The header strip names the columns on a wide screen; the per-box label
     only appears once the rows stack. */
  .poster-rows__label {
    display: none;
    font-size: 0.76rem;
    font-weight: 600;
    color: var(--muted);
    margin-bottom: 3px;
  }

  .poster-rows__row .input,
  .poster-rows__row .select {
    padding: 7px 9px;
    font-size: 0.92rem;
  }

  .poster-rows__remove {
    padding: 4px 8px;
    font-size: 1.15rem;
    line-height: 1;
    justify-self: center;
  }

  /* The hint follows the Add button, so it has to break onto its own line. */
  .poster-rows__hint {
    display: block;
    margin-top: 10px;
  }

  .poster-rows__toggle {
    float: right;
    font-weight: 400;
    padding: 1px 8px;
  }

  /* One subject's heading and the points under it. */
  .poster-group {
    border: 1px solid var(--line);
    border-radius: var(--radius-sm);
    padding: 10px;
    margin-bottom: 10px;
    background: var(--brand-50);
  }

  .poster-group__head {
    display: flex;
    gap: 8px;
    align-items: center;
    margin-bottom: 8px;
  }

  .poster-group .textarea {
    min-height: 0;
  }

  @media (max-width: 900px) {
    .poster-rows__head {
      display: none;
    }

    .poster-rows__row {
      grid-template-columns: 1fr 1fr 34px !important;
      padding: 10px;
      border: 1px solid var(--line);
      border-radius: var(--radius-sm);
    }

    .poster-rows__label {
      display: block;
    }

    .poster-rows__remove {
      align-self: end;
    }
  }

  /* The "make a poster" row above the file picker in the new-post dialog. */
  .poster-pick {
    display: flex;
    align-items: center;
    gap: 10px;
    flex-wrap: wrap;
    margin-bottom: 10px;
  }

  .poster__meta {
    margin-top: 8px;
    text-align: center;
  }

  /* A poster longer than one page previews as the images it will become. */
  .poster__page {
    margin: 0 0 16px;
  }

  /* Short fields pair off, so a date and a time do not take two rows. */
  .poster-pair {
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: 12px;
  }

  .poster-pair .field {
    min-width: 0;
  }

  /* The embellishments - a note, a closing line - start folded away. */
  .poster-more {
    border-top: 1px solid var(--line);
    margin-top: 18px;
    padding-top: 12px;
  }

  .poster-more > summary {
    cursor: pointer;
    font-size: 0.85rem;
    font-weight: 600;
    color: var(--brand-700);
    list-style: none;
  }

  .poster-more > summary::-webkit-details-marker {
    display: none;
  }

  .poster-more > summary::before {
    content: "+ ";
  }

  .poster-more[open] > summary::before {
    content: "− ";
  }

  .poster-more__body {
    margin-top: 14px;
  }

  .attachment-list {
    list-style: none;
    margin: 0;
    padding: 0;
    display: grid;
    gap: 10px;
  }

  .attachment {
    display: flex;
    align-items: center;
    gap: 12px;
    padding: 10px;
    border: 1px solid var(--line);
    border-radius: var(--radius-sm);
  }

  .attachment__thumb {
    flex: none;
    width: 56px;
    height: 56px;
    object-fit: cover;
    border-radius: 6px;
    background: var(--brand-50);
  }

  .attachment__thumb--file {
    display: grid;
    place-items: center;
    background: var(--brand-700);
    color: #fff;
    font-size: 0.66rem;
    font-weight: 700;
  }

  .attachment__info {
    flex: 1;
    min-width: 0;
    display: flex;
    flex-direction: column;
    gap: 2px;
  }

  .attachment__info a {
    font-weight: 600;
    word-break: break-all;
  }

  .attachment__confirm {
    display: flex;
    gap: 6px;
  }

  /* ------------------------------------------------------------- mobile */
  /* Below this width the five-column table cannot be read, so each row
     becomes a card and the action buttons come within thumb reach. */
  @media (max-width: 700px), (pointer: coarse) {
    .modal__close {
      width: 44px;
      height: 44px;
      padding: 0;
      flex: none;
    }
  }

  @media (max-width: 700px) {
    .table-scroll {
      overflow-x: visible;
      background: none;
      border: none;
      box-shadow: none;
      border-radius: 0;
    }

    .admin-table {
      display: block;
      min-width: 0;
      font-size: 0.95rem;
    }

    .admin-table thead {
      position: absolute;
      width: 1px;
      height: 1px;
      margin: -1px;
      padding: 0;
      overflow: hidden;
      clip: rect(0 0 0 0);
      white-space: nowrap;
      border: 0;
    }

    .admin-table tbody,
    .admin-table tr,
    .admin-table td {
      display: block;
      width: 100%;
    }

    .admin-table tr {
      background: var(--paper);
      border: 1px solid var(--line);
      border-radius: var(--radius);
      box-shadow: var(--shadow-sm);
      padding: 14px 16px;
      margin-bottom: 12px;
    }

    .admin-table td {
      border-bottom: none;
      padding: 5px 0;
    }

    /* Cells that lost their column header carry it inline instead. */
    .admin-table td[data-label] {
      display: flex;
      align-items: center;
      justify-content: space-between;
      gap: 12px;
    }

    .admin-table td[data-label]::before {
      content: attr(data-label);
      font-size: 0.72rem;
      letter-spacing: 0.07em;
      text-transform: uppercase;
      font-weight: 700;
      color: var(--muted);
    }

    .admin-table__nowrap {
      white-space: normal;
      text-align: right;
    }

    /* On a card the menu button sits on its own line, at the right. */
    .admin-table__actions {
      justify-content: flex-end;
      margin-top: 10px;
    }

    .row-menu {
      /* Comfortably tappable, unlike the tight desktop button. */
      padding: 8px 16px;
    }

    .admin-table__actions .btn {
      flex: 1 1 0;
      min-width: 0;
    }

    .admin-bar__actions {
      width: 100%;
    }

    .admin-bar__actions .btn {
      flex: 1 1 auto;
    }

    .searchbar .input {
      max-width: none;
      flex: 1 1 100%;
    }

    .searchbar .select {
      flex: 1 1 45%;
      min-width: 0;
    }
  }

  @media (max-width: 900px) {
    .poster {
      grid-template-columns: minmax(0, 1fr);
    }

    /* Stacked, the preview leads so the words being typed are visible. */
    .poster__preview {
      position: static;
      order: -1;
    }

  }

  @media (max-width: 640px) {
    .modal {
      padding: 10px;
      align-items: flex-end;
    }

    .modal__panel {
      max-height: calc(100dvh - 20px);
    }

    .attachment {
      flex-wrap: wrap;
    }

    .upload-panel__row {
      flex-direction: column;
      gap: 0;
    }

    .upload-panel__row .field {
      margin-bottom: 14px;
      width: 100%;
    }
  }
</style>
